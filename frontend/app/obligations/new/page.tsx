"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { DashboardGate } from "@/components/auth/dashboard-gate";
import { Card, PageHeader } from "@/components/ui/surface";
import { PrimaryButton } from "@/components/ui/primary-button";
import { TextField } from "@/components/ui/text-field";
import { createObligation, getCurrentUser, getUsers, type OrganizationUser } from "@/lib/api";

type Form = { title: string; description: string; matter: string; regulatory_source: string; article: string; deadline: string; frequency: string; compliance_status: "PENDING" | "IN_PROGRESS" | "COMPLIANT" | "OVERDUE"; responsible_user_id: string };
const initial: Form = { title: "", description: "", matter: "", regulatory_source: "", article: "", deadline: "", frequency: "", compliance_status: "PENDING", responsible_user_id: "" };
const fieldClass = "h-12 w-full rounded-lg border border-slate-300 bg-white px-3 text-[15px] text-slate-950 outline-none transition hover:border-slate-400 focus-visible:border-slate-900 focus-visible:ring-4 focus-visible:ring-slate-900/10";

export default function NewObligationPage() {
  const router = useRouter();
  const [form, setForm] = useState<Form>(initial); const [users, setUsers] = useState<OrganizationUser[]>([]); const [allowed, setAllowed] = useState(false); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  useEffect(() => { getCurrentUser().then((user) => { if (user.role !== "ADMIN") { router.replace("/obligations"); return; } setAllowed(true); return getUsers().then(setUsers); }).catch(() => router.replace("/login")); }, [router]);
  const set = (key: keyof Form, value: string) => setForm((previous) => ({ ...previous, [key]: value }));
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setError(""); setLoading(true); try { const obligation = await createObligation({ ...form, description: form.description || null, article: form.article || null, deadline: form.deadline || null, frequency: form.frequency || null, responsible_user_id: form.responsible_user_id || null }); router.push(`/obligations/${obligation.id}`); } catch (caught) { setError(caught instanceof Error ? caught.message : "No fue posible crear la obligación."); } finally { setLoading(false); } }
  if (!allowed) return <DashboardGate><Card className="mx-auto max-w-2xl p-8 text-sm text-slate-600" role="status">Verificando permisos…</Card></DashboardGate>;
  return <DashboardGate><div className="mx-auto max-w-4xl space-y-8"><PageHeader title="Crear obligación" description="Registra la información base de la obligación ambiental." action={<Link href="/obligations" className="text-sm font-medium text-slate-700 underline-offset-4 hover:text-slate-950 hover:underline">Volver a obligaciones</Link>} /><Card className="p-5 sm:p-6"><form onSubmit={submit} noValidate><div className="grid gap-5 sm:grid-cols-2"><TextField id="title" label="Título" required value={form.title} onChange={(event) => set("title", event.target.value)} /><TextField id="matter" label="Materia" required value={form.matter} onChange={(event) => set("matter", event.target.value)} /><TextField id="regulatory-source" label="Fuente normativa" required value={form.regulatory_source} onChange={(event) => set("regulatory_source", event.target.value)} /><TextField id="article" label="Artículo" value={form.article} onChange={(event) => set("article", event.target.value)} /><TextField id="deadline" label="Fecha límite" type="date" value={form.deadline} onChange={(event) => set("deadline", event.target.value)} /><TextField id="frequency" label="Periodicidad" value={form.frequency} onChange={(event) => set("frequency", event.target.value)} /><div><label htmlFor="responsible" className="mb-2 block text-sm font-medium text-slate-800">Responsable</label><select id="responsible" value={form.responsible_user_id} onChange={(event) => set("responsible_user_id", event.target.value)} className={fieldClass}><option value="">Sin asignar</option>{users.filter((user) => user.is_active && (user.role === "ADMIN" || user.role === "RESPONSIBLE")).map((user) => <option key={user.id} value={user.id}>{user.name} · {user.role}</option>)}</select></div><div><label htmlFor="status" className="mb-2 block text-sm font-medium text-slate-800">Estado</label><select id="status" value={form.compliance_status} onChange={(event) => set("compliance_status", event.target.value)} className={fieldClass}><option value="PENDING">Pendiente</option><option value="IN_PROGRESS">En proceso</option><option value="COMPLIANT">Cumplida</option><option value="OVERDUE">Vencida</option></select></div><div className="sm:col-span-2"><label htmlFor="description" className="mb-2 block text-sm font-medium text-slate-800">Descripción</label><textarea id="description" value={form.description} onChange={(event) => set("description", event.target.value)} className="min-h-32 w-full rounded-lg border border-slate-300 bg-white p-3 text-[15px] text-slate-950 outline-none transition placeholder:text-slate-400 hover:border-slate-400 focus-visible:border-slate-900 focus-visible:ring-4 focus-visible:ring-slate-900/10" /></div></div>{error && <div className="mt-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800" role="alert">{error}</div>}<div className="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:items-center sm:justify-end"><Link href="/obligations" className="text-center text-sm font-medium text-slate-700 underline-offset-4 hover:text-slate-950 hover:underline">Cancelar</Link><div className="w-full sm:w-auto"><PrimaryButton className="sm:w-auto" isLoading={loading} loadingLabel="Creando…">Crear obligación</PrimaryButton></div></div></form></Card></div></DashboardGate>;
}
